/**
 * Same dead-end page the wedding gallery uses — its copy already says
 * "slavlja", not "venčanja", so it fits a birthday scan verbatim. Kept as a
 * re-export so the offer stays written in one place.
 */
export { default } from "@/app/pozivnica/[slug]/galerija/not-found";
